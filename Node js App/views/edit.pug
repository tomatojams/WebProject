doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Document
    link(rel="stylesheet", href="/main.css")
  body.grey-bg
    include nav.pug

    form.form-box(action=`/edit/${postAlias._id}`, method="POST")
      h4 수정
      input(name="title", type="text", value= postAlias.title)
      input(name="content", type="text" , value =postAlias.content)

      button(type="submit") 전송
