doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Document
    link(rel="stylesheet", href="/main.css")
  body.grey-bg
    include nav.pug
    div.write-post-border
      form.postform(action="/post", method="POST")
        h4 글쓰기
        input#write-title(name="title", type="text")
        input#write-content(name="content", type="text")

        button.simplebutton-log(type="submit") 전송
